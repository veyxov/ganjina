[package]
name = "web"
version = "0.1.0"
edition = "2024"

[dependencies]
vault-core = { path = "../core" }
photos = { path = "../photos" }
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "net", "macros"] }
